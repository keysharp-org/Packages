{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://packages.keysharp.org/schemas/version.schema.json",
  "title": "Release manifest",
  "description": "packages/<owner>/<name>/versions/<version>-r<revision>.json — one immutable release. Once merged this file is never edited or deleted; a mistake is corrected by publishing a new revision.",
  "type": "object",
  "required": ["schema", "name", "owner", "version", "revision", "published", "entry", "engines", "platforms", "artifacts"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": 1 },
    "name": { "type": "string", "pattern": "^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$" },
    "owner": { "type": "string", "pattern": "^[A-Za-z0-9]([A-Za-z0-9._-]*[A-Za-z0-9])?$" },
    "version": {
      "type": "string",
      "description": "SemVer. The human-facing number; packaging fixes bump revision instead."
    },
    "revision": {
      "type": "integer",
      "minimum": 1,
      "description": "Counts repackagings of the same source. Revisions run 1,2,3 with no gaps."
    },
    "published": { "type": "string", "format": "date-time" },
    "entry": {
      "type": "string",
      "description": "Archive-relative path of the file a consumer includes, e.g. src/FindText.ks."
    },
    "engines": {
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "description": "Which engines this release runs on. Every claim must pass that engine's validation in CI — compatibility is earned, not declared. Values use the #Requires comparison grammar (>=0.0.0.17), NOT SemVer ranges.",
      "properties": {
        "keysharp": { "type": "string" },
        "autohotkey": { "type": "string" }
      }
    },
    "platforms": {
      "type": "array",
      "minItems": 1,
      "items": { "enum": ["any", "win-x64", "win-arm64", "linux-x64", "linux-arm64", "osx-x64", "osx-arm64"] }
    },
    "capabilities": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Capability names as #Requires capability spells them. Informational: tooling displays them, nothing enforces them yet."
    },
    "dependencies": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "owner/name to an npm-style SemVer range (^1.4.0). Deliberately a different grammar from engines."
    },
    "source": { "$ref": "#/$defs/sourceRef" },
    "artifacts": {
      "type": "object",
      "minProperties": 1,
      "description": "One entry per platform. One logical version across platforms: platform-only repackaging is a revision bump, never a version fork.",
      "additionalProperties": {
        "type": "object",
        "required": ["sha256", "size", "sources"],
        "additionalProperties": false,
        "properties": {
          "sha256": {
            "type": "string",
            "pattern": "^[0-9a-f]{64}$",
            "description": "The artifact's identity. Mirrors need no trust: bytes that do not hash to this are rejected wherever they came from."
          },
          "size": { "type": "integer", "minimum": 1 },
          "sources": {
            "type": "array",
            "items": { "type": "string", "format": "uri" },
            "description": "Places the same bytes can be fetched from, tried in order. Never identity."
          }
        }
      }
    }
  },
  "$defs": {
    "sourceRef": {
      "type": "object",
      "description": "Provenance: where this release's content came from. Not a download location.",
      "additionalProperties": false,
      "properties": {
        "kind": { "enum": ["ports", "git", "scripthub", "forum"] },
        "repository": { "type": "string" },
        "commit": { "type": "string" },
        "path": { "type": "string" },
        "url": { "type": "string" },
        "upstream": { "$ref": "#/$defs/sourceRef" }
      }
    }
  }
}
